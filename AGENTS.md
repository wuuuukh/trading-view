# AGENTS.md

## 1. 專案定位

本專案是台股多代理人模擬交易決策系統，核心目標是透過資料、規則、Agent 判斷與複盤流程，進行可回測、可解釋、可控的模擬交易決策。

此專案的重點不是讓 AI 自由發明策略，而是讓系統依照既有交易規則、SOP 與模組責任分工，輸出一致、可追蹤的研究與模擬交易結果。

本檔案是給 Codex、AI Agent 與開發者看的專案工作規範。它不取代交易策略文件，也不覆蓋原本的規則來源。

## 2. Codex 工作原則

- 修改前必須先理解需求，不可看到檔案就直接動手改。
- 修改前必須確認相關文件，先找出本次需求對應的規則來源與模組說明。
- 優先採用最小修改，只改能完成需求的最小範圍。
- 不要重構整個專案，除非任務明確要求。
- 不要刪除既有功能，除非任務明確要求。
- 不要自行新增未要求的大型架構、流程或抽象層。
- 不要把策略規則和開發規範混在一起；策略內容回到既有規則文件，協作方式寫在本檔。
- 不確定時先回報判斷、假設與風險，不要直接亂改。
- 若需求涉及交易邏輯，預設以保守方式處理，不自行放寬規則。
- 若需求涉及多模組，先說明影響範圍，再開始修改。

## 3. 修改前必讀文件

| 修改類型 | 必讀文件 | 用途 |
|---|---|---|
| 修改交易規則 | `config/rules.yaml`、`交易方式_SOP.txt`、`交易邏輯_操作思維.txt` | 確認主規則、操作限制、核心交易思維，避免改壞原策略 |
| 修改多 Agent 架構 | `docs/multi_agent_architecture.md` | 確認模組責任邊界、資料流與不可變更的架構限制 |
| 修改單一 Agent | `docs/agents/` 對應檔案 | 確認該 Agent 的輸入、輸出、責任與限制 |
| 修改大盤 / 指數判斷 | `docs/agents/01_index_agent.md` | 確認大盤總開關邏輯與 operation mode 邊界 |
| 修改候選股初篩 | `docs/agents/02_candidate_scanner.md` | 確認候選股分層、初篩邏輯與舊 Agent 對應 |
| 修改策略路由 | `docs/agents/03_strategy_router.md` | 確認個股應交由哪個 Agent 處理，不混淆路由責任 |
| 修改長線 / 波段規則 | `docs/agents/04_long_swing_agent.md` | 確認續抱、加碼、減碼、出場與長抱限制 |
| 修改盤中判斷 | `docs/agents/05_intraday_agent.md` | 確認盤中切入、等待、排除條件與盤中責任範圍 |
| 修改風控 / 最終決策 | `docs/agents/06_decision_risk_controller.md` | 確認風控優先權、最終決策限制與不可越權條件 |
| 修改報表輸出 | `docs/agents/07_reporter.md` | 確認 JSON、CSV、Markdown、Dashboard 等輸出責任 |
| 修改績效追蹤 / 複盤 | `docs/agents/08_performance_tracker.md` | 確認績效觀察、追蹤維度與複盤輸出定位 |
| 修改前端或網頁 | 先找相關 `HTML / CSS / JS` 檔案，再視需要參考 `docs/` 相關頁面 | 先確認實際畫面來源與結構，不可直接全專案重構 |

## 4. 修改流程

### 第一階段：分析

1. 確認需求。
2. 找出相關檔案。
3. 判斷最小修改範圍。
4. 說明預計修改方式。

### 第二階段：修改

1. 只修改與需求直接相關的檔案。
2. 不順手改其他問題。
3. 不改動交易策略，除非任務明確要求。
4. 不改動 `rules.yaml`，除非任務明確要求。

### 第三階段：回報

1. 列出修改檔案。
2. 說明修改內容。
3. 說明是否影響其他模組。
4. 說明是否需要測試。
5. 說明下一步建議。

## 5. Agent 文件維護規則

- `docs/agents/` 下每個檔案只描述自己的 Agent。
- 不要把所有規則塞進單一 Agent 文件。
- `05_intraday_agent.md` 只放盤中進出場判斷。
- `04_long_swing_agent.md` 只放長線 / 波段規則。
- `06_decision_risk_controller.md` 只放風控與決策限制。
- `01_index_agent.md` 只處理大盤與市場狀態，不負責選股或下單。
- `02_candidate_scanner.md` 只負責候選股初篩與分層，不直接承擔最終買賣決策。
- `03_strategy_router.md` 只負責路由，不負責盤中進場或長線續抱細節。
- `07_reporter.md` 只負責輸出與整理，不應承載交易判斷本體。
- `08_performance_tracker.md` 只負責追蹤與複盤，不應直接改寫策略。
- Daily Review / 今日複盤如果尚未有獨立文件，應建議新增，不要硬塞進 intraday agent。

## 6. 交易策略修改限制

- 不可讓 LLM 直接決定真實下單。
- 系統目前以模擬交易、回測、複盤為主。
- 策略調整必須可追蹤、可回測、可解釋。
- 不可只因單日結果就直接大幅修改策略。
- 所有規則變更都應留下原因。
- 若要修改策略邏輯，必須先回看 `config/rules.yaml`、`交易方式_SOP.txt`、`交易邏輯_操作思維.txt`。
- 若只是修報表、前端、資料流程，不應順手改交易規則。

## 7. 前端 / 網頁修改規範

- 修改網頁前先找出相關 `HTML / CSS / JS`。
- 外觀問題優先改 `CSS`。
- 不要為了樣式問題亂改 `JS`。
- 修改前需確認是否影響手機版 RWD。
- 一次只改一個區塊。
- 不要邊想邊大範圍重構。
- 若頁面資料來自報表生成腳本，先確認資料來源，再決定改前端還是改輸出流程。
- 若同時涉及 `docs/`、`site/`、首頁輸出，先釐清哪一份是來源檔、哪一份是產物檔。

## 8. 禁止事項

- 禁止未經要求重構整個專案。
- 禁止刪除既有功能。
- 禁止無理由修改 `rules.yaml`。
- 禁止把模擬交易寫成真實下單。
- 禁止把長線、波段、當沖、複盤規則混在同一文件。
- 禁止沒有說明就修改多個模組。
- 禁止修改與需求無關的檔案。
- 禁止在沒有確認規則來源前，自行新增策略條件。
- 禁止用「順手整理」名義擴大修改範圍。

## 9. 每次任務完成後回報格式

本次修改摘要：
- 需求理解：
- 修改檔案：
- 修改內容：
- 未修改的部分：
- 是否影響其他模組：
- 是否需要測試：
- 下一步建議：

## 10. 任務分級與工具使用控制

每次任務開始前，Codex 必須先判斷任務等級，再決定搜尋範圍、讀檔深度與是否需要先提計畫。

### 小型任務

例如：
- 改文字
- 改顏色
- 改間距
- 修單一頁面的小 Bug

規則：
- 只找直接相關檔案。
- 不掃整個專案。
- 不修改架構。
- 優先一次完成。

### 中型任務

例如：
- 新增一個區塊
- 修改一個 Agent 的輸入輸出
- 調整一段資料流程
- 修正報表產生邏輯

規則：
- 先讀 `AGENTS.md`。
- 再讀對應模組文件。
- 列出影響範圍。
- 修改前先回報計畫。

### 大型任務

例如：
- 新增 Agent
- 改多 Agent 架構
- 調整交易流程
- 修改多個模組
- 大幅改前端版面

規則：
- 不可直接修改。
- 必須先提出修改計畫。
- 必須列出會影響的檔案。
- 必須等使用者確認後才開始。
- 優先拆成多個小任務執行。

若使用者連續提出多個零碎修改，Codex 應主動提醒：

「這些需求可以先整理成一批，再一次修改，會比較省工具使用量，也比較不容易改壞專案。」
